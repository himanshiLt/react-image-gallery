---
framework: jest
preRun:
     command:
        - yarn install
preMerge:
  pattern:
    - "./tests/*.test.js"
postMerge:
  pattern: 
    - "./tests/*.test.js"
postRun:
     command:
        - yarn --version
configFile: jest.config.js
version: 1.0
